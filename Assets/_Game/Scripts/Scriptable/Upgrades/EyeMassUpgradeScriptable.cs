using UnityEngine;

[CreateAssetMenu(fileName = "EyeMassUpgrade", menuName = "Data/Upgrade/EyeSpeedData")]
public class EyeMassUpgradeScriptable : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
